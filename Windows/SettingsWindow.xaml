<?xml version="1.0" encoding="utf-8"?>
<Window mc:Ignorable="d"
        x:Class="BluDay.FluentNoiseRemover.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:communityToolkit="using:CommunityToolkit.WinUI"
        xmlns:communityToolkitControls="using:CommunityToolkit.WinUI.Controls"
        xmlns:controls="using:BluDay.FluentNoiseRemover.Controls"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- System backdrop -->
    <Window.SystemBackdrop>
        <MicaBackdrop Kind="Base"/>
    </Window.SystemBackdrop>
    
    <!-- Root grid -->
    <Grid>
        <!-- Resources -->
        <Grid.Resources>
            <Style x:Key="HeaderTextBlockStyle"
                   BasedOn="{StaticResource TitleTextBlockStyle}"
                   TargetType="TextBlock">
                <Setter Property="FontSize" Value="24"/>
                <Setter Property="Margin" Value="32,16,32,24"/>
            </Style>
            
            <Style x:Key="SectionHeaderGridStyle" TargetType="Grid">
                <Setter Property="Margin" Value="4,4,4,8"/>
            </Style>
            
            <Style x:Key="SectionHeaderIconViewboxStyle" TargetType="Viewbox">
                <Setter Property="Height" Value="14"/>
                <Setter Property="Margin" Value="0,0,8,0"/>
                <Setter Property="Width" Value="14"/>
            </Style>

            <Style x:Key="SectionHeaderTextBlockStyle"
                   BasedOn="{StaticResource BodyStrongTextBlockStyle}"
                   TargetType="TextBlock">
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="TextLineBounds" Value="Tight"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
        </Grid.Resources>
        
        <!-- Row defintions -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- Title bar -->
        <controls:TitleBar x:Name="TitleBarControl" Grid.Row="0"/>
        
        <!-- Content -->
        <Grid MaxWidth="800" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock x:Uid="SettingsWindow/HeaderTextBlock"
                       Style="{StaticResource HeaderTextBlockStyle}"/>

            <ScrollViewer Padding="32,0" Grid.Row="1">
                <StackPanel Orientation="Vertical" Spacing="16">
                    <!-- Audio -->
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        
                        <!-- Header -->
                        <Grid Style="{StaticResource SectionHeaderGridStyle}" Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>

                            <Viewbox Style="{StaticResource SectionHeaderIconViewboxStyle}" Grid.Column="0">
                                <FontIcon Glyph="&#xe8d6;"/>
                            </Viewbox>

                            <TextBlock x:Uid="SettingsWindow/Audio/HeaderTextBlock"
                                       Style="{StaticResource SectionHeaderTextBlockStyle}"
                                       Grid.Column="1"/>
                        </Grid>

                        <!-- Expanders -->
                        <StackPanel Spacing="2" Grid.Row="1">
                            <communityToolkitControls:SettingsCard x:Uid="SettingsWindow/Audio/DefaultNoisePreset/SettingsCard"
                                                                   HeaderIcon="{communityToolkit:FontIcon Glyph=&#xe9e9;}">
                                <ComboBox ItemsSource="{x:Bind LocalizedNoisePresets.Values}"
                                          SelectedIndex="0"/>
                            </communityToolkitControls:SettingsCard>
                        </StackPanel>
                    </Grid>

                    <!-- Interface -->
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        
                        <!-- Header -->
                        <Grid Style="{StaticResource SectionHeaderGridStyle}" Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>

                            <Viewbox Style="{StaticResource SectionHeaderIconViewboxStyle}" Grid.Column="0">
                                <FontIcon Glyph="&#xf8a6;"/>
                            </Viewbox>

                            <TextBlock x:Uid="SettingsWindow/Interface/HeaderTextBlock"
                                       Style="{StaticResource SectionHeaderTextBlockStyle}"
                                       Grid.Column="1"/>
                        </Grid>

                        <!-- Expanders -->
                        <StackPanel Spacing="2" Grid.Row="1">
                            <!-- Application theme -->
                            <communityToolkitControls:SettingsCard x:Uid="SettingsWindow/Interface/ApplicationTheme/SettingsCard"
                                                                   HeaderIcon="{communityToolkit:FontIcon Glyph=&#xe790;}">
                                <ComboBox ItemsSource="{x:Bind LocalizedApplicationThemes.Values}"
                                          SelectedIndex="0"/>
                            </communityToolkitControls:SettingsCard>

                            <!-- System backdrop -->
                            <communityToolkitControls:SettingsCard x:Uid="SettingsWindow/Interface/SystemBackdrop/SettingsCard"
                                                                   HeaderIcon="{communityToolkit:FontIcon Glyph=&#xe81e;}">
                                <ComboBox ItemsSource="{x:Bind LocalizedSystemBackdrops.Values}"
                                          SelectedIndex="0"/>
                            </communityToolkitControls:SettingsCard>
                            
                            <!-- Language -->
                            <communityToolkitControls:SettingsCard x:Uid="SettingsWindow/Interface/Language/SettingsCard"
                                                                   HeaderIcon="{communityToolkit:FontIcon Glyph=&#xf2b7;}">
                                <ComboBox ItemsSource="{x:Bind LocalizedLanguages.Values}"
                                          SelectedIndex="0"/>
                            </communityToolkitControls:SettingsCard>
                        </StackPanel>
                    </Grid>

                    <!-- About -->
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        
                        <!-- Header -->
                        <Grid Style="{StaticResource SectionHeaderGridStyle}" Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>

                            <Viewbox Style="{StaticResource SectionHeaderIconViewboxStyle}" Grid.Column="0">
                                <FontIcon Glyph="&#xe946;"/>
                            </Viewbox>

                            <TextBlock x:Uid="SettingsWindow/About/HeaderTextBlock"
                                       Style="{StaticResource SectionHeaderTextBlockStyle}"
                                       Grid.Column="1"/>
                        </Grid>

                        <!-- Expanders -->
                        <StackPanel Spacing="2" Grid.Row="1">
                            <communityToolkitControls:SettingsExpander x:Uid="SettingsWindow/About/ApplicationInfo/SettingsExpander"
                                                                       HeaderIcon="{communityToolkit:BitmapIcon Source=ms-appx:///Assets/StoreLogo.scale-100.png}">
                                <!-- Content -->
                                <communityToolkitControls:SettingsExpander.Content>
                                    <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               Text="1.0"/>
                                </communityToolkitControls:SettingsExpander.Content>

                                <!-- Items -->
                                <communityToolkitControls:SettingsExpander.Items>
                                    <communityToolkitControls:SettingsCard Header="Session identifier: {0}"/>
                                </communityToolkitControls:SettingsExpander.Items>
                            </communityToolkitControls:SettingsExpander>
                        </StackPanel>
                    </Grid>

                    <!-- Hyperlink buttons -->
                    <StackPanel Margin="0,0,0,32" Spacing="4">
                        <!-- Reset preferences -->
                        <HyperlinkButton x:Uid="SettingsWindow/HyperlinkButtons/RepositoryOnGitHub"/>

                        <!-- Send feedback -->
                        <HyperlinkButton x:Uid="SettingsWindow/HyperlinkButtons/SendFeedback"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
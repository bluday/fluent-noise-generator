<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="using:FluentNoiseGenerator.Features.Playback.UI.Controls">
    <Style x:Key="PlaybackControlPanelButtonStyle"
           BasedOn="{StaticResource DefaultButtonStyle}"
           TargetType="Button">
        <Setter Property="Background" Value="{ThemeResource PlaybackControlPanelButtonBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource PlaybackControlPanelButtonBorderBrush}"/>
        <Setter Property="BorderThickness" Value="{ThemeResource PlaybackControlPanelButtonBorderThickness}"/>
        <Setter Property="Height" Value="{ThemeResource PlaybackControlPanelButtonSize}"/>
        <Setter Property="Width" Value="{ThemeResource PlaybackControlPanelButtonSize}"/>
        <Setter Property="Padding" Value="{ThemeResource PlaybackControlPanelButtonPadding}"/>
    </Style>

    <Style TargetType="controls:PlaybackControlPanel">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:PlaybackControlPanel">
                    <StackPanel HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Orientation="Horizontal"
                                Spacing="{ThemeResource PlaybackControlPanelSpacing}">
                        <!-- Presets button -->
                        <Button Style="{ThemeResource PlaybackControlPanelButtonStyle}">
                            <Button.Flyout>
                                <Flyout AreOpenCloseAnimationsEnabled="{ThemeResource PlaybackControlPanelFlyoutAnimationsEnabled}"
                                        ShouldConstrainToRootBounds="{ThemeResource PlaybackControlPanelFlyoutConstrainToRootBounds}">
                                    <Grid Width="{ThemeResource PlaybackControlPanelPresetsFlyoutWidth}">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition/>
                                        </Grid.RowDefinitions>

                                        <Grid Grid.Row="0"
                                              Margin="{ThemeResource PlaybackControlPanelPresetsHeaderMargin}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock x:Uid="/PlaybackWindow/TextBlocks/ControlPanel/NoisePresets"
                                                       FontSize="{ThemeResource PlaybackControlPanelHeaderFontSize}"
                                                       Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                        </Grid>

                                        <!-- Temporary stuff -->
                                        <ListView Grid.Row="1"
                                                  Grid.ColumnSpan="2">
                                            <ListViewItem>
                                                <Grid Margin="{ThemeResource PlaybackControlPanelPresetItemMargin}">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="{ThemeResource PlaybackControlPanelPresetItemSpacing}"/>
                                                        <ColumnDefinition/>
                                                    </Grid.ColumnDefinitions>

                                                    <Border Grid.Column="0"
                                                            Width="{ThemeResource PlaybackControlPanelPresetColorSize}"
                                                            Height="{ThemeResource PlaybackControlPanelPresetColorSize}"
                                                            HorizontalAlignment="Left"
                                                            VerticalAlignment="Center"
                                                            Background="CornflowerBlue"
                                                            CornerRadius="{ThemeResource PlaybackControlPanelPresetCornerRadius}"/>

                                                    <TextBlock Grid.Column="2"
                                                               VerticalAlignment="Center"
                                                               Text="Blue"/>
                                                </Grid>
                                            </ListViewItem>

                                            <ListViewItem>
                                                <Grid Margin="{ThemeResource PlaybackControlPanelPresetItemMargin}">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="{ThemeResource PlaybackControlPanelPresetItemSpacing}"/>
                                                        <ColumnDefinition/>
                                                    </Grid.ColumnDefinitions>

                                                    <Border Grid.Column="0"
                                                            Width="{ThemeResource PlaybackControlPanelPresetColorSize}"
                                                            Height="{ThemeResource PlaybackControlPanelPresetColorSize}"
                                                            HorizontalAlignment="Left"
                                                            VerticalAlignment="Center"
                                                            Background="SaddleBrown"
                                                            CornerRadius="{ThemeResource PlaybackControlPanelPresetCornerRadius}"/>

                                                    <TextBlock Grid.Column="2"
                                                               VerticalAlignment="Center"
                                                               Text="Brown"/>
                                                </Grid>
                                            </ListViewItem>

                                            <ListViewItem>
                                                <Grid Margin="{ThemeResource PlaybackControlPanelPresetItemMargin}">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="{ThemeResource PlaybackControlPanelPresetItemSpacing}"/>
                                                        <ColumnDefinition/>
                                                    </Grid.ColumnDefinitions>

                                                    <Border Grid.Column="0"
                                                            Width="{ThemeResource PlaybackControlPanelPresetColorSize}"
                                                            Height="{ThemeResource PlaybackControlPanelPresetColorSize}"
                                                            HorizontalAlignment="Left"
                                                            VerticalAlignment="Center"
                                                            Background="White"
                                                            CornerRadius="{ThemeResource PlaybackControlPanelPresetCornerRadius}"/>

                                                    <TextBlock Grid.Column="2"
                                                               VerticalAlignment="Center"
                                                               Text="White"/>
                                                </Grid>
                                            </ListViewItem>
                                        </ListView>
                                    </Grid>
                                </Flyout>
                            </Button.Flyout>

                            <Viewbox>
                                <FontIcon Glyph="{ThemeResource PlaybackControlPanelPresetsGlyph}"/>
                            </Viewbox>
                        </Button>

                        <!-- Playback button -->
                        <Button Width="{ThemeResource PlaybackControlPanelPlaybackButtonSize}"
                                Height="{ThemeResource PlaybackControlPanelPlaybackButtonSize}"
                                Command="{TemplateBinding PlaybackButtonClickCommand}"
                                CornerRadius="{ThemeResource PlaybackControlPanelPlaybackButtonCornerRadius}"
                                Style="{StaticResource DefaultButtonStyle}">
                            <Viewbox>
                                <FontIcon x:Name="PlaybackButtonFontIcon"
                                          Glyph="{ThemeResource PlaybackControlPanelPlayGlyph}"/>
                            </Viewbox>
                        </Button>

                        <!-- Volume button -->
                        <Button Style="{ThemeResource PlaybackControlPanelButtonStyle}">
                            <Button.Flyout>
                                <Flyout AreOpenCloseAnimationsEnabled="{ThemeResource PlaybackControlPanelFlyoutAnimationsEnabled}"
                                        ShouldConstrainToRootBounds="{ThemeResource PlaybackControlPanelFlyoutConstrainToRootBounds}">
                                    <StackPanel Orientation="Horizontal"
                                                Spacing="{ThemeResource PlaybackControlPanelVolumeFlyoutSpacing}">

                                        <Slider Width="{ThemeResource PlaybackControlPanelVolumeSliderWidth}"
                                                Value="{Binding Volume, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>

                                        <StackPanel Width="{ThemeResource PlaybackControlPanelVolumeValueWidth}"
                                                    VerticalAlignment="Center"
                                                    Orientation="Horizontal">
                                            <TextBlock Text="{Binding Volume, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                                            <TextBlock Text="%"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Flyout>
                            </Button.Flyout>

                            <Viewbox>
                                <FontIcon Glyph="{ThemeResource PlaybackControlPanelVolumeGlyph}"/>
                            </Viewbox>
                        </Button>

                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>

                                <VisualState x:Name="Playing">
                                    <VisualState.Setters>
                                        <Setter Target="PlaybackButtonFontIcon.Glyph" Value="{ThemeResource PlaybackControlPanelPauseGlyph}"/>
                                    </VisualState.Setters>

                                    <VisualState.StateTriggers>
                                        <StateTrigger IsActive="{Binding IsPlaying, RelativeSource={RelativeSource TemplatedParent}}"/>
                                    </VisualState.StateTriggers>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
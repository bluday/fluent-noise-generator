<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="using:FluentNoiseGenerator.Features.Settings.UI.Controls">
    <Style TargetType="controls:SettingsSectionHeader">
        <Setter Property="Margin" Value="{ThemeResource SettingsSectionHeaderMargin}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:SettingsSectionHeader">
                    <Grid Margin="{TemplateBinding Margin}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>

                        <Viewbox Grid.Column="0"
                                 Width="{ThemeResource SettingsSectionHeaderIconSize}"
                                 Height="{ThemeResource SettingsSectionHeaderIconSize}"
                                 Margin="{ThemeResource SettingsSectionHeaderIconMargin}">
                            <ContentPresenter Content="{TemplateBinding Icon}"/>
                        </Viewbox>

                        <TextBlock Grid.Column="1"
                                   VerticalAlignment="Center"
                                   FontSize="{ThemeResource SettingsSectionHeaderFontSize}"
                                   Style="{StaticResource BodyStrongTextBlockStyle}"
                                   Text="{TemplateBinding Text}"
                                   TextLineBounds="Tight"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="BluDay.FluentNoiseRemover.Controls.SettingsSectionHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <!-- Resources -->
    <UserControl.Resources>
        <Style x:Key="SectionHeaderTextBlockStyle"
               BasedOn="{StaticResource BodyStrongTextBlockStyle}"
               TargetType="TextBlock">
        </Style>
    </UserControl.Resources>
    
    <!-- Layout root -->
    <Grid Margin="4,4,4,8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Viewbox Height="14" Margin="0,0,8,0" Width="14" Grid.Column="0">
            <FontIcon x:Name="HeaderFontIcon" Glyph="&#xe8d6;"/>
        </Viewbox>

        <TextBlock x:Name="HeaderTextBlock"
                   FontSize="14"
                   TextLineBounds="Tight"
                   Style="{StaticResource BodyStrongTextBlockStyle}"
                   VerticalAlignment="Center"
                   Grid.Column="1"/>
    </Grid>
</UserControl>
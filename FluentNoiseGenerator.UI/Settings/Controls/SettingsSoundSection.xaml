<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="FluentNoiseGenerator.UI.Settings.Controls.SettingsSoundSection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:communityToolkit="using:CommunityToolkit.WinUI"
             xmlns:communityToolkitControls="using:CommunityToolkit.WinUI.Controls"
             xmlns:controls="using:FluentNoiseGenerator.UI.Settings.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:DefaultBindMode="OneWay"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <controls:SettingsSectionHeader x:Uid="/SettingsWindow/SectionHeaders/Sound"
                                        Grid.Row="0"
                                        Glyph="&#xe8d6;"/>

        <StackPanel Grid.Row="1"
                    Spacing="2">
            <communityToolkitControls:SettingsCard x:Uid="/SettingsWindow/SettingsCards/Sound/SampleRate"
                                                   HeaderIcon="{communityToolkit:FontIcon Glyph=&#xf61f;}">
                <ComboBox ItemsSource="{x:Bind AvailableAudioSampleRates}"
                          SelectedItem="{x:Bind SelectedAudioSampleRate, Mode=TwoWay}"/>
            </communityToolkitControls:SettingsCard>
        </StackPanel>
    </Grid>
</UserControl>